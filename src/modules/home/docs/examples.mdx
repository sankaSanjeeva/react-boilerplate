import { Link } from 'react-router-dom';
import { OpenIcon } from '@/assets/icons';

# Examples

## Users

These examples highlight the management of common components, states, routing, forms, etc.

* <Link to="/users/list" className="inline-flex items-center gap-2">Users <OpenIcon className="w-5 h-5" /></Link>
* <Link to="/users/create" className="inline-flex items-center gap-2">Create a new user <OpenIcon className="w-5 h-5" /></Link>
